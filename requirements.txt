# ItinerAI-Bench — Python dependencies
# Install: pip install -r requirements.txt
# Python 3.11+

# ── Core ──────────────────────────────────────────────────────────────────────
pydantic>=2.7,<3
python-dotenv>=1.0
tqdm>=4.66
httpx>=0.27

# ── Phase 1: Data Generation ──────────────────────────────────────────────────
openai>=1.30                # Phase 1 teacher (gpt-4o-mini) + Phase 2 OpenAI-compat client
google-genai>=1.0           # Phase 1 optional Gemini provider (default provider is openai)
groq>=0.9                   # Phase 1 Groq provider (fallback) + Phase 2 Groq agent option

# ── Phase 2: Agents & MCP Servers ────────────────────────────────────────────
mcp>=1.0.0,<2.0.0
fastapi>=0.115
uvicorn[standard]>=0.30
openrouteservice>=2.3
ddgs>=9.0                   # web search (renamed from duckduckgo-search)

# ── Phase 2: API caching ──────────────────────────────────────────────────────
diskcache>=5.6

# ── Phase 3: Training (run on Colab T4 / Lightning.ai A100, NOT locally) ─────
# Trained models are in models/finetune/, models/distill/, models/curriculum/
# To re-train, upload data/training/*.jsonl and run the Colab notebooks.
# Install on Colab/Lightning only:
#   pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
#   pip install trl>=0.9 datasets>=2.20 transformers>=4.44 peft>=0.12 accelerate bitsandbytes

# ── Phase 4: Evals ────────────────────────────────────────────────────────────
sentence-transformers>=3.0  # local intent alignment (no API)

# ── Phase 5: Inference API ────────────────────────────────────────────────────
# fastapi, uvicorn, pydantic, httpx already listed above under Core/Phase 2